{
  "name": "@genuinegrads/zk-service",
  "version": "1.0.0",
  "description": "Zero-Knowledge proof circuits and artifacts for GenuineGrads",
  "type": "module",
  "scripts": {
    "compile": "bash scripts/compile.sh",
    "setup": "bash scripts/setup.sh",
    "export-vkey": "bash scripts/export-vkey.sh",
    "build": "npm run compile && npm run setup && npm run export-vkey",
    "test": "mocha test/**/*.test.js --timeout 60000",
    "clean": "rm -rf build/"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.4"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "mocha": "^10.2.0"
  }
}
